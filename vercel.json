{
  "buildCommand": "bun run start",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/app.js",
      "methods": ["GET", "POST", "PATCH", "PUT", "DELETE", "OPTIONS"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
